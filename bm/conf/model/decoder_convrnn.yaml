# @package _global_

model_name: convrnn
convrnn:
  hidden:
    meg: 512
  bidirectional_lstm: true

dset:
  features:
    - WordSegment

optim:
  loss: regression_classification

task:
  type: decode
